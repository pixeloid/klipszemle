{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Accomodation</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ entity.name }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ entity.address }}</td>
            </tr>
            <tr>
                <th>Pricesingle</th>
                <td>{{ entity.priceSingle }}</td>
            </tr>
            <tr>
                <th>Pricedouble</th>
                <td>{{ entity.priceDouble }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('accomodation') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('accomodation_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
