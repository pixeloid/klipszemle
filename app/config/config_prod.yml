imports:
    - { resource: config.yml }

#framework:
#    validation:
#        cache: apc

doctrine:
    dbal:
        host:     "localhost"
        dbname:   "mgyaitt"
        user:     "mgyaitt"
        password: "mgyaitt2015"

monolog:
    handlers:
        main:
            type:         fingers_crossed
            action_level: error
            handler:      nested
        nested:
            type:  stream
            path:  "%kernel.logs_dir%/%kernel.environment%.log"
            level: debug
        console:
            type:  console
