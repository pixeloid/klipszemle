imports:
    - { resource: config.yml }

#framework:
#    validation:
#        cache: apc

doctrine:
    dbal:
        host:     "localhost"
        dbname:   "mgygt2015"
        user:     "mgygt2015"
        password: "bn3ffct6n987"

monolog:
    handlers:
        main:
            type:         fingers_crossed
            action_level: error
            handler:      nested
        nested:
            type:  stream
            path:  "%kernel.logs_dir%/%kernel.environment%.log"
            level: debug
        console:
            type:  console
