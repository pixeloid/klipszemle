{% block craue_flow_stepList_class %}
	{%- if loop.index == flow.getCurrentStepNumber() -%}
		{{ ' class="craue_formflow_current_step btn btn-primary"' }}
	{%- elseif flow.isStepSkipped(loop.index) -%}
		{{ ' class="craue_formflow_skipped_step btn btn-primary"' }}
	{%- elseif flow.isStepDone(loop.index) -%}
		{{ ' class="craue_formflow_done_step btn btn-primary"' }}
	{%- else -%}
		{{ ' class="btn btn-default"' }}
	{%- endif -%}
{% endblock %}

{% block craue_flow_stepLabel %}
	{{- stepLabel | trans -}}
{% endblock %}
