{% extends 'base.html.twig' %}

{% block body -%}
    <h1>Accomodation list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Address</th>
                <th>Pricesingle</th>
                <th>Pricedouble</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('accomodation_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.address }}</td>
                <td>{{ entity.priceSingle }}</td>
                <td>{{ entity.priceDouble }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('accomodation_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('accomodation_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('accomodation_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
